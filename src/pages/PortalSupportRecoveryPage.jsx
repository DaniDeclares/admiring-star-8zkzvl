import React,{useState}from'react';import{Link}from'react-router-dom';import'./PortalAccessPage.css';
export default function PortalSupportRecoveryPage(){const token=new URLSearchParams(window.location.search).get('token')||'';const[email,setEmail]=useState(''),[requested,setRequested]=useState(false),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState('');
const request=async e=>{e.preventDefault();setBusy(true);setError('');try{const response=await fetch('/api/provider-support-recovery',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'request',email})});if(!response.ok)throw new Error('Recovery request could not be sent.');setRequested(true);}catch(e2){setError(e2.message);}finally{setBusy(false);}};
const submit=async e=>{e.preventDefault();setError('');if(password.length<8)return setError('Use a password with at least 8 characters.');if(password!==confirm)return setError('Passwords do not match.');if(!token)return setError('This recovery link is invalid.');setBusy(true);try{const response=await fetch('/api/provider-support-recovery',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||'Password recovery could not be completed.');setDone(true);window.history.replaceState({},document.title,window.location.pathname);}catch(e2){setError(e2.message);}finally{setBusy(false);}};
if(!token){if(requested)return <main className="portal-access"><div className="portal-success-card"><p className="portal-kicker">CHECK YOUR EMAIL</p><h1>Secure recovery email requested</h1><p>If an active provider account exists for that email, DANI sent a one-time recovery link. The link opens DANI first and is not consumed by an email scanner.</p><div className="portal-success-actions"><Link className="portal-primary" to="/portal/login">Return to sign in</Link></div></div></main>;return <main className="portal-access"><div className="portal-form-card portal-login-card"><p className="portal-kicker">SECURE ACCOUNT RECOVERY</p><h1>Recover your provider account</h1><p>Use this recovery path if the standard reset email does not open the DANI password screen.</p><form onSubmit={request}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label>{error&&<div className="portal-error" role="alert">{error}</div>}<button className="portal-primary portal-submit" disabled={busy}>{busy?'Sending…':'Send secure recovery email'}</button></form><p className="portal-existing"><Link to="/portal/login">Back to sign in</Link></p></div></main>;}
if(done)return <main className="portal-access"><div className="portal-success-card"><p className="portal-kicker">PASSWORD UPDATED</p><h1>Your DANI password is ready</h1><p>Your one-time recovery link has been consumed. Sign in with the password you just chose.</p><div className="portal-success-actions"><Link className="portal-primary" to="/portal/login">Sign in</Link></div></div></main>;
return <main className="portal-access"><div className="portal-form-card portal-login-card"><p className="portal-kicker">SECURE ACCOUNT RECOVERY</p><h1>Choose your new password</h1><p>This one-time DANI support link lets you choose your own password. Your password is submitted securely to DANI's account service and is not displayed to DANI staff.</p><form onSubmit={submit}><label>New password<input type="password" minLength="8" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/></label><label>Confirm new password<input type="password" minLength="8" required value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password"/></label>{error&&<div className="portal-error" role="alert">{error}</div>}<button className="portal-primary portal-submit" disabled={busy||!token}>{busy?'Updating…':'Set my password'}</button></form></div></main>;}