/*
 * LEGACY TEST QUARANTINE (2026-09-05)
 *
 * These assertions target superseded pricing IDs and the former B2C/B2B
 * catalog. The active authority is the governed commercial catalog plus
 * masterCommercialResolver. Keep this file until the legacy connector is
 * migrated or retired; do not use stale IDs to force the current resolver
 * back into the old architecture.
 */

describe.skip('DDOS pricing connector — legacy catalog migration pending', () => {});
